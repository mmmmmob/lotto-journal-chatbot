{
  "name": "@lotto/db",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "main": "src/client.ts",
  "types": "src/client.ts",
  "private": "true",
  "exports": {
    ".": {
      "import": "./src/client.ts",
      "types": "./src/client.ts"
    }
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "dotenv -e .env -- prisma migrate dev",
    "postinstall": "prisma generate",
    "validate": "dotenv -e .env -- prisma validate",
    "studio": "dotenv -e .env -- prisma studio",
    "build": "echo \"(db has nothing to build)\"",
    "lint": "pnpm -w exec eslint . --ext ts --ext js",
    "typecheck": "tsc -p tsconfig.json --noEmit || true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.17.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.21",
    "dotenv-cli": "^10.0.0",
    "prettier-plugin-prisma": "^5.0.0",
    "prisma": "^6.17.1"
  }
}
