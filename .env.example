# Environment configuration for the Lotto Journal monorepo.
# Copy this file to `.env.local` and adjust the values to match your setup.

# Use the format for connection string on DB explorer as : postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
DB_USERNAME="lotto"
DB_PASSWORD="password"
DB_NAME="lotto_db"
