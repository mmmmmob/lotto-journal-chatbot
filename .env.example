# Environment configuration for the Lotto Journal monorepo.
# Copy this file to `.env.local` and adjust the values to match your setup.

# Connection string for the Prisma client in `packages/db`.
# Use the format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://lotto:password@localhost:5432/lotto_db"
